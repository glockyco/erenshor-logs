namespace ErenshorLogs.Protocol;

/// <summary>
/// WebSocket protocol version.
/// </summary>
public static class ProtocolVersion
{
  /// <summary>
  /// Current protocol version (0.x indicates breaking changes expected).
  /// </summary>
  public const string Current = "0.1.0";
}
